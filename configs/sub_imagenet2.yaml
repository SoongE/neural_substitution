defaults:
  - config
  - override train/optimizer: lamb
  - _self_

gpus: [ 0,1,2,3 ]
name: ${model.model_name}

info:
  project: Neural_Substitution_ImageNet_ForResNet50

train:
  epochs: 100
  batch_size: 128
  num_workers: 32
  bce_loss: True
  target_thresh: 0.2

  lr_scheduler:
    warmup_epochs: 5
    warmup_lr: 1e-6
    min_lr: 1e-5

  optimizer:
    grad_accumulation: 2
    lr: 3.5e-3
    weight_decay: 1e-2

model:
  model_name:
  drop_path_rate: 0.15
  neural_drop_rate: 0.0

dataset:
  train_size: [ 3,160,160 ]
  augmentation:
    mixup: 0.1
    cutmix: 1.0
    crop_pct: 0.95
    aa: rand-m7-mstd0.5-inc1
    aug_repeats: 3
    smoothing: 0.0